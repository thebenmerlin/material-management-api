# Server
PORT=3000
NODE_ENV=production

# Auth
JWT_SECRET=replace-with-a-strong-random-string

# Database (SQLite path relative to /api)
DB_PATH=./db/material_management.db

# File uploads (local folder)
UPLOAD_DIR=./uploads

# CORS (only used if your code reads this)
ALLOWED_ORIGINS=*
